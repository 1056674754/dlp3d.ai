/**
 * Future: FCM / local notifications with a channel (e.g. @notifee/react-native).
 * For now the app only requests POST_NOTIFICATIONS (Android 13+) so later push
 * can be wired without another permission prompt flow.
 */
export const NOTIFICATION_CHANNEL_ID = 'dlp3d_default';
